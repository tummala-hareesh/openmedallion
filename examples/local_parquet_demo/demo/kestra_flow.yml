id: demo
namespace: openmedallion.projects
description: "Bronze → Silver → Gold pipeline for demo."

tasks:

  - id: run_bronze
    type: io.kestra.plugin.scripts.python.Commands
    warningOnStdErr: false
    commands:
      - medallion run demo --layer bronze
    workingDirectory: /app/examples/local_parquet_demo

  - id: run_silver
    type: io.kestra.plugin.scripts.python.Commands
    warningOnStdErr: false
    commands:
      - medallion run demo --layer silver
    workingDirectory: /app/examples/local_parquet_demo
    dependsOn:
      - run_bronze

  - id: run_gold
    type: io.kestra.plugin.scripts.python.Commands
    warningOnStdErr: false
    commands:
      - medallion run demo --layer gold
    workingDirectory: /app/examples/local_parquet_demo
    dependsOn:
      - run_silver

# Uncomment to enable a scheduled refresh:
# triggers:
#   - id: daily_refresh
#     type: io.kestra.plugin.core.trigger.Schedule
#     cron: "0 6 * * *"   # every day at 06:00 UTC
